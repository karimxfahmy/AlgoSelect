// minimal placeholder — the real UI lands in part 6.
// keeping this here so `npm run dev` shows something sensible right after
// the scaffold step.
export default function App() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <div className="text-center max-w-md">
        <h1 className="text-3xl font-semibold mb-2">AlgoSelect</h1>
        <p className="text-slate-400">
          Multi-algorithm decision engine. UI coming together — this is just
          the scaffold.
        </p>
      </div>
    </div>
  )
}
